basePath: /
definitions:
  httpapi.ErrorResponse:
    properties:
      error:
        type: string
    type: object
  httpapi.ProductResponse:
    properties:
      id:
        type: string
      name:
        type: string
      price:
        type: number
    type: object
info:
  contact: {}
  description: DDD example with **Swagger/OpenAPI** (not "twigger"). Generated via
    swaggo/swag.
  title: Swagger/OpenAPI catalog example
  version: "1.0"
paths:
  /v1/products/{id}:
    get:
      description: Returns a catalog product
      parameters:
      - description: Product ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/httpapi.ProductResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/httpapi.ErrorResponse'
      summary: Get product by ID
      tags:
      - catalog
swagger: "2.0"
